# Pinned versions guarantee reproducible CI builds.
# Bump deliberately via Dependabot or `pip install -U <pkg>` + manual verification.
feedparser>=6.0.12,<7.0
requests>=2.34.2,<3.0
Pillow>=12.2.0,<13.0
edge-tts>=7.2.8,<8.0
PyYAML>=6.0.3,<7.0
# OpenCV (headless) for face-aware b-roll cropping. ~35 MB install,
# adds ~10 s to cold-start. Used only inside utils/face_crop.py — that
# module no-ops gracefully when the import fails, so OpenCV is
# technically optional and the requirements pin lets us drop it
# without code changes if we ever need to slim cold-start back down.
opencv-python-headless>=4.13.0.92,<5.0
# libsodium bindings — used by upload_tiktok._persist_token_to_github_secret
# to encrypt the rotated TikTok refresh_token before pushing it back to
# the TIKTOK_TOKEN repository secret via the GitHub Actions Secrets API.
# Without this the channel breaks within 24h of every manual re-auth,
# because TikTok single-use-rotates refresh tokens on every refresh.
pynacl>=1.6.2,<2.0
