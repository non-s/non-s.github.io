# Development and quality-assurance dependencies for non-s.github.io / Amber Hours.
# Pin versions to match .pre-commit-config.yaml and .github/workflows/quality-gate.yml.

# Test runner and coverage
pytest>=8,<10
pytest-cov>=5,<8

# Linting and formatting
ruff>=0.6,<1
black>=26.3.1,<27

# Static type checking
mypy>=1.10,<2

# Security scanning
bandit>=1.8,<2
pip-audit>=2.9,<3

# Git hooks (optional; install with `pre-commit install`)
pre-commit>=4.0,<5
