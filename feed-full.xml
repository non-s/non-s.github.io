---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:media="http://search.yahoo.com/mrss/"
     xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ site.title | xml_escape }} — Full Content</title>
    <link>{{ site.url }}</link>
    <description>{{ site.description | xml_escape }}</description>
    <language>en-US</language>
    <atom:link href="{{ '/feed-full.xml' | absolute_url }}" rel="self" type="application/rss+xml"/>
    <lastBuildDate>{{ site.time | date_to_rfc822 }}</lastBuildDate>
    {% for post in site.posts limit:20 %}
    <item>
      <title>{{ post.title | xml_escape }}</title>
      <link>{{ post.url | absolute_url }}</link>
      <description>{{ post.description | default: post.excerpt | strip_html | xml_escape }}</description>
      <content:encoded><![CDATA[{{ post.content }}]]></content:encoded>
      <pubDate>{{ post.date | date_to_rfc822 }}</pubDate>
      <guid isPermaLink="true">{{ post.url | absolute_url }}</guid>
      {% if post.image %}<media:content url="{{ post.image }}" medium="image"/>{% endif %}
      {% for cat in post.categories %}<category>{{ cat | xml_escape }}</category>{% endfor %}
      {% if site.author.email and site.author.email != "" %}<author>{{ site.author.email }} ({{ site.author.name | default: site.title }})</author>{% endif %}
    </item>
    {% endfor %}
  </channel>
</rss>
