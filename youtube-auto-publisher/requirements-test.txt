# ============================================================
# Test-only dependencies for the Quality workflow.
#
# Kept separate from requirements.txt on purpose: the full production
# requirements file pulls in heavy media-processing libraries (moviepy,
# openai-whisper, opencv-python, librosa, ...) that are unnecessary for
# importing src/ and running the unit tests in tests/. Installing only
# these packages keeps CI fast while still exercising the real
# googleapiclient/requests/loguru/dotenv behavior the tests depend on.
# ============================================================
pytest>=7.4.0
requests>=2.31.0
python-dotenv>=1.0.0
loguru>=0.7.2
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1
