# ============================================================
# .github/workflows/fetch-news.yml
# GitHub Actions — Auto news fetcher
#
# Runs 5x per day (6h, 10h, 14h, 18h, 22h UTC). Only commits if there are new posts.
# Duplicate detection is handled by fetch_news.py itself
# (checks existing filenames before creating new ones).
# ============================================================

name: "📰 Fetch News RSS"

on:
  schedule:
    # Every 3 hours (8 runs/day). Hourly was over the Actions free
    # tier (2000 min/month); 8×~10min ≈ 80 min/day = ~2400 min/month,
    # but we share that quota with youtube-bot/newsletter/etc, and on
    # public repos Actions minutes are unmetered anyway.
    - cron: "0 */3 * * *"

  workflow_dispatch:
    inputs:
      debug:
        description: "Enable debug log?"
        required: false
        default: "false"
        type: boolean

# Serialise with every other workflow that pushes to main, otherwise the
# pull-rebase retry below would race against youtube-bot / weekly-digest
# commits. `cancel-in-progress: false` means later runs wait.
concurrency:
  group: main-write
  cancel-in-progress: false

jobs:
  fetch-and-publish:
    name: "Fetch & publish news"
    runs-on: ubuntu-latest
    timeout-minutes: 90

    permissions:
      contents: write

    steps:

      - name: "📥 Checkout repository"
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: "🐍 Set up Python 3.11"
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"

      - name: "📦 Install dependencies"
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: "✅ Validate Python syntax"
        run: |
          python -m py_compile fetch_news.py
          python -m py_compile utils/text.py utils/dedup.py utils/ai_helper.py utils/retry.py
          python -m py_compile submit_urls.py generate_search_index.py
          echo "✅ Syntax OK"

      - name: "📡 Run fetch_news.py"
        run: python fetch_news.py
        env:
          PYTHONIOENCODING: utf-8
          PYTHONUNBUFFERED: "1"
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}

      - name: "🔍 Check for new posts"
        id: check_changes
        run: |
          CHANGED=$(git status --porcelain _posts/ | wc -l | tr -d ' ')
          echo "changed=$CHANGED" >> $GITHUB_OUTPUT
          echo "New posts found: $CHANGED"
          if [ "$CHANGED" -gt "0" ]; then
            echo "New posts:"
            git status --porcelain _posts/ | head -20
          fi

      - name: "💾 Commit & push new posts"
        if: steps.check_changes.outputs.changed != '0'
        run: |
          git config user.name  "GlobalBR News Bot"
          git config user.email "bot@globalbrnews.com"

          git add _posts/
          git add _tags/ || true
          git add assets/images/posts/ || true
          git add fetch_news.log || true
          git add search-index.json || true
          # Per-run state: ETag/Last-Modified cache + dead-feed counters.
          # Tracked so they survive the fresh CI checkout next run.
          git add _data/feed_cache.json _data/feed_health.json 2>/dev/null || true

          ADDED=$(git diff --cached --name-only | grep "_posts/" | wc -l | tr -d ' ')
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M UTC")
          CATEGORIES=$(git diff --cached --name-only | grep "_posts/" | head -5 | xargs -I{} grep -m1 "^categories:" {} 2>/dev/null | sed 's/categories: \[//;s/\].*//' | tr -d ' ' | cut -d',' -f1 | sort -u | head -3 | tr '\n' ',' | sed 's/,$//')

          if [ -n "$CATEGORIES" ]; then
            MSG="🤖 +${ADDED} post(s) [${CATEGORIES}] — ${TIMESTAMP}"
          else
            MSG="🤖 +${ADDED} post(s) — ${TIMESTAMP}"
          fi

          git commit -m "$MSG

          Auto-fetched via fetch_news.py
          Workflow: ${{ github.workflow }} #${{ github.run_number }}"

          # Other workflows (youtube-bot, weekly-audit, etc.) may have
          # pushed to main during our 20-30 minute run. Pull-rebase
          # before pushing so we don't lose this batch to a rejected push.
          for attempt in 1 2 3; do
            if git push origin HEAD; then
              echo "✅ $ADDED post(s) published (attempt $attempt)!"
              break
            fi
            echo "⚠️ Push rejected (attempt $attempt) — pulling rebase and retrying"
            git pull --rebase --autostash origin main || true
            sleep 3
          done
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: "🔎 Google Search Console — indexar URLs novas"
        if: steps.check_changes.outputs.changed != '0'
        continue-on-error: true
        run: python google_index.py
        env:
          GOOGLE_INDEXING_CREDENTIALS: ${{ secrets.GOOGLE_INDEXING_CREDENTIALS }}

      - name: Generate tag pages
        if: steps.check_changes.outputs.changed != '0'
        run: python generate_tag_pages.py

      - name: Generate search index
        if: steps.check_changes.outputs.changed != '0'
        run: python generate_search_index.py

      - name: "📰 Daily Briefing (original editorial roundup)"
        if: steps.check_changes.outputs.changed != '0'
        continue-on-error: true
        run: python create_daily_briefing.py
        env:
          PYTHONIOENCODING: utf-8
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}

      - name: "🗓️ Weekly Wrap-up (Fridays only)"
        if: steps.check_changes.outputs.changed != '0'
        continue-on-error: true
        run: python create_weekly_wrapup.py
        env:
          PYTHONIOENCODING: utf-8
          MISTRAL_API_KEY: ${{ secrets.MISTRAL_API_KEY }}

      - name: "🔊 Generate audio narration (edge-tts)"
        if: steps.check_changes.outputs.changed != '0'
        continue-on-error: true
        run: python generate_audio.py
        env:
          PYTHONIOENCODING: utf-8

      - name: "🏛️ Generate category pillar pages"
        if: steps.check_changes.outputs.changed != '0'
        continue-on-error: true
        run: python generate_pillar_pages.py

      - name: "💾 Commit derived artefacts (briefings, audio, search index)"
        if: steps.check_changes.outputs.changed != '0'
        continue-on-error: true
        run: |
          git add search-index.json _tags/ 2>/dev/null || true
          git add _posts/*-daily-briefing.md _posts/*-weekly-wrapup.md 2>/dev/null || true
          # generate_audio.py writes back into _posts/ frontmatter + drops MP3s in assets/audio/
          git add assets/audio/posts/ _posts/*.md 2>/dev/null || true
          # Pillar pages live under {category}/pillar.html
          git add */pillar.html 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "ℹ️ No derived artefact changes."
          else
            git commit -m "🤖 derived artefacts — $(date -u +'%Y-%m-%d %H:%M UTC')"
            git push origin HEAD || echo "⚠️ push failed, will retry next run"
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: "🔵 Bing & IndexNow — submeter URLs novas"
        if: steps.check_changes.outputs.changed != '0'
        continue-on-error: true
        run: python submit_urls.py
        env:
          BING_API_KEY:  ${{ secrets.BING_API_KEY }}
          INDEXNOW_KEY:  ${{ secrets.INDEXNOW_KEY }}
          SITE_URL:      "https://non-s.github.io"

      - name: "ℹ️ No new posts"
        if: steps.check_changes.outputs.changed == '0'
        run: echo "✨ No new posts this run — skipping commit."

      - name: "📋 Save execution log"
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: "fetch-news-log-${{ github.run_number }}"
          path: fetch_news.log
          retention-days: 7
          if-no-files-found: ignore
