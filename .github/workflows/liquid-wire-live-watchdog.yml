name: "Liquid Wire - Continuous Live Watchdog"

on:
  workflow_dispatch:
  # GitHub cron delivery is best-effort and can be delayed under load. Every
  # completed live runner therefore invokes the same idempotent recovery check
  # immediately; an already-dispatched replacement makes this a no-op.
  workflow_run:
    workflows: ["Liquid Wire - Continuous Live"]
    types: [completed]
  schedule:
    # Recover a broken chain within at most 10 minutes.
    - cron: "7,17,27,37,47,57 * * * *"

permissions:
  contents: read
  actions: write

concurrency:
  group: liquid-wire-live-watchdog
  cancel-in-progress: true

jobs:
  ensure-live:
    name: "Ensure continuous live session exists"
    if: ${{ vars.LIQUID_WIRE_ENABLED == '1' && vars.LIQUID_WIRE_PAUSE_SCHEDULES != '1' }}
    runs-on: ubuntu-latest
    steps:
      - name: "Inspect and recover live chain"
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          active=$(gh run list \
            --repo "${{ github.repository }}" \
            --workflow liquid-wire-live.yml \
            --limit 20 \
            --json status \
            --jq '[.[] | select(.status == "in_progress" or .status == "queued" or .status == "pending")] | length')
          if [ "$active" -eq 0 ]; then
            gh workflow run liquid-wire-live.yml \
              --repo "${{ github.repository }}" \
              --ref main \
              -f duration_minutes=330 \
              -f privacy=public \
              -f continuous=true \
              -f chaos_after_seconds=0
            echo "Continuous live chain recovered."
          else
            echo "Continuous live session already active or queued."
          fi
